The below instruction are regarding the Lab 1 of compressible flow (SG2215).

	1 - Put in a same file the 3 documents : "Data.mat", "fonction.m" and "Lab_1.m" ;

	1 bis - While the first on contains the data from "Exp1", "Exp2" and "Exp3", the two other one are defining the main code.

	2 - You must run the first sub-section first in the Matlab code. Then, you should be able to run any of the other sub-section (separated each by "%%").